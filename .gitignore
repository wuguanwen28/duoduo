node_modules
dist
dist-ssr
*.local
.DS_Store

.vscode/
!.vscode/settings.json
.claude/

# Rust / Tauri build output (huge, generated)
src-tauri/target/
target/

# 打包产物（pnpm app:build 生成的绿色版 zip）
dist-package/
# 镜像中转目录（publish:mirrors 从 GitHub Release 下载的唯一构建产物）
dist-mirror/

# Generated Tauri files
src-tauri/gen/schemas/

# 注：src/types/auto-imports.d.ts 与 components.d.ts 是 unplugin 生成的类型声明。
# 因 vue-tsc 在 vite 之前运行，CI 干净检出时它们尚未生成会导致类型检查失败，
# 故改为提交进仓库（新增自动导入的 API / 组件后需本地重新生成并一并提交）。
